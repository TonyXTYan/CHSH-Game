"""
Dashboard functionality for the CHSH Game.

This module now serves as a compatibility layer that imports all functionality
from the dashboard package to maintain backward compatibility.
"""

# Import everything from the dashboard package to maintain compatibility
from .dashboard import *
